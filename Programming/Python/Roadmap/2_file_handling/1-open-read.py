# create file

open(file_path, access_mode)

x - create new file. if file already exists it will throw an error.
w - create new file. if file already exist it will truncates existing file.
a - open a file in apend mode.



f= open('sample.csv',"x")

